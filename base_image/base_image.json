{
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "ap-southeast-1",
    "source_ami": "ami-f068a193",
    "instance_type": "t2.micro",
    "ssh_username": "centos",
    "ssh_pty": true,
    "ami_name": "twu51ll base {{timestamp}}"
  }],
  "provisioners": [{
    "type": "chef-solo",
    "cookbook_paths": ["cookbooks"],
    "run_list": [
      "base::default"
    ]
  }]
}
